Fund -> Details expanding menu:
Add Headers to columns

Market -> Leaderboard
Change trader name column to the following format
{Avatar} CharacterName username
For the character name, remove the server/realm name (the -suffix)
For the username, leave it the same color, but if the user has an attached character name, then set the username to muted
